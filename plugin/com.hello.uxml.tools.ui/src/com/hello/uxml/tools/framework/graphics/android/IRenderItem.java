package com.hello.uxml.tools.framework.graphics.android;

/**
 * Implements interface for persistent render list items.
 *
 * @author ferhat
 *
 */
public interface IRenderItem {
  void draw(RenderContext context);
}
